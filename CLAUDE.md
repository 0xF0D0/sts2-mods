# 작업 규칙

## 모델 역할 분담 (필수)

**Fable/Opus는 설계·분석·검증만 한다. 실제 코드 작성은 Sonnet이 한다.**

- 설계, 디컴파일 소스 조사, 디버깅 원인 분석, 테스트 실행·검증, 코드 리뷰 → 상위 모델이 직접 수행
- 파일 생성 / 대규모 수정 등 구현 → `Agent` 툴에 `model: "sonnet"`으로 위임
  - 위임 프롬프트에 설계 결정을 명시할 것: 파일 경로, 클래스/메서드 시그니처,
    자료구조, 제약 조건 (예: 리플렉션 문자열은 `decompiled/`에서 확인된 것만 사용)
- 예외 (직접 해도 됨): 1~2줄 수정, 빌드 에러 즉시 수정, 문서·설정 파일 편집
- 사용자가 매번 지시하지 않아도 기본으로 적용한다.

## API를 스펙에 넣기 전 확인 (필수)

이 세션에서 같은 실수를 네 번 했다. 전부 **선언 줄만 보고 단정**한 것이다.

- **호출처를 전수 grep한다.** 어떤 메서드의 호출처가 `GameAction.ExecuteAction` 안뿐이면
  그건 **수신부지 송신부가 아니다.** 멀티에서 상태를 바꾸려면 그 액션을
  `ActionQueueSynchronizer.RequestEnqueue`로 넣어야 한다.
  - 실제 사고: `MapSelectionSynchronizer.PlayerVotedForMapCoord`, `PlayerCmd.EndTurn`을
    직접 호출 → 각 피어가 자기 표만 보고 영원히 대기.
- **접근자까지 읽는다.** `public bool Foo { get; private set; }`는 선언 줄만 보면 public이다.
  - 실제 사고: `CombatManager.PlayerActionsDisabled`를 "public settable"로 스펙에 적음.
- **`else` 분기까지 읽고 판단한다.**
  - 실제 사고: `BeginRunAsync`가 `BootstrapInMultiplayer` 게이트에 걸린 것만 보고
    "릴리스 빌드에서 죽었다"고 결론 → 바로 아래 `else`에 정상 경로가 있었다.
- **카탈로그와 풀을 구분한다.** `ModelDb.All*` / `Debug*`는 "게임에 정의된 전부"지
  "이 런에서 나올 수 있는 것"이 아니다. 게임 자신의 팩토리(`RelicFactory`,
  `PotionFactory`, `CardFactory`, 액트별 인카운터 풀)에서 뽑는다.
  - 실제 사고: 퍼저가 불가능한 상태를 만들어 없는 버그를 "바닐라 버그"로 문서에 박음.

## 원인 단정 금지

- 크래시 스택은 "어디서 터졌나"만 말한다. **"이 상태가 정당한가"는 따로 확인한다.**
  외부(게임) 버그라고 쓰기 전에 "실제 플레이 경로에서 이 값이 이렇게 될 수 있나"를 증명한다.
  못 하면 "내 쪽 입력이 비정상일 가능성"을 먼저 적는다.
- 수정이 원인이었다는 주장은 **A/B로만** 한다. 같은 시드로 끄고 켜서 재현/소멸을 보인다.
- 진단 전에 **런이 내가 생각한 그 런인지** 확인한다: 잔존 프로세스 없는지, 로그가
  이번 실행의 것인지(시간순 말고 pid로 특정), 머신이 절전에 안 들어갔는지.
  - 실제 사고: 절전 17분을 "코드가 느리다"로, 포트 충돌을 "하네스 버그"로,
    낡은 로그를 "수정이 안 통했다"로 오독.

## 검증은 자기 자신을 의심하게 만든다

- **아무 일도 안 일어나서 아무 문제도 안 생긴 것**을 통과로 읽지 않는다.
  검사는 "그 코드가 실제로 실행됐다"를 먼저 증명하고 나서 결과를 말해야 한다.
  - 실제 사고: 복원 0회인데 "SyncOrbNodes 검증됨", 복원 커밋 0회인데 "SUCCESS".
- 메커니즘이 돌았는지는 **카운터로 증명**한다. 게임플레이 우연에 기대지 않는다.
  - 실제 사고: 컨테이너 복사가 한 번도 안 일어난 런을 보고 수정이 됐다고 믿을 뻔함 →
    시작 시 자체 검사(probe 객체)로 결정적으로 증명하게 바꿈.
- "전수조사했다"고 쓰기 전에 **무엇을 안 봤는지** 센다. 기각한 것도 개수와 근거를 남긴다.

## 범위·릴리스 판단은 사용자 것

- 릴리스 여부·범위, 무엇을 미룰지는 **사용자가 정한다.** 근거와 선택지를 제시하고 멈춘다.
- "어떻게 할거야 / ~할까?"는 실행 지시가 아니라 **계획 요청**이다. 글로 답하고 멈춘다.

## 게임 인스턴스 (머신당 1주체)

- 실행 직전 `pgrep -fl "Slay the Spire 2"`로 확인한다. 남의 인스턴스가 보이면
  **띄우기 전에** 메시지로 조율한다. 종료는 광범위 `pkill` 금지, 자기 인자로만
  (`pgrep -f "undosync-mpfuzz" | xargs -r kill`).
- 장시간 런은 `caffeinate -dimsu`로 감싼다 (절전이 결과를 오염시킨다).
- 로그는 시간순이 아니라 **실행한 pid로 특정**한다.
- ENet 33771은 하나뿐이다. 멀티 하네스는 동시 실행이 원천적으로 불가능하다.
